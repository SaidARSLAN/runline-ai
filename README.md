# Runline AI

> _Placeholder README — to be hand-written. No AI-generated content for this file._

Manufacturing operator copilot. Multi-agent system built with LangGraph.

Documentation coming soon.

---

**Note for early cloners:** this repo currently depends on a sibling
`paperdex` package (used as the retrieval layer) that is not yet on PyPI.
Until paperdex is published, runline-ai is developed inside a monorepo
alongside paperdex. A standalone install path will arrive with the proper
README rewrite.
